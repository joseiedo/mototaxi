---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .env
  - infra/redpanda-init.sh
  - observability/prometheus.yml
  - observability/grafana/provisioning/datasources/prometheus.yaml
  - observability/grafana/provisioning/dashboards/dashboards.yaml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-05

must_haves:
  truths:
    - "Running `docker compose up --build -d` starts all infrastructure services without error"
    - "The `driver.location` Redpanda topic is created with PUSH_SERVER_REPLICAS × PARTITION_MULTIPLIER partitions before any application service starts"
    - "All tunable parameters are documented in `.env.example` with inline comments"
    - "Changing a value in `.env` takes effect on the next `docker compose up` without editing any other file"
    - "Services that depend on Redis wait for its healthcheck to pass before starting"
    - "Services that depend on the Redpanda topic wait for the init container to exit 0 before starting"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full infrastructure service orchestration"
      contains: "redpanda, redis, redpanda-init, redpanda-console, prometheus, grafana, kafka-exporter, cadvisor, nginx"
    - path: "infra/redpanda-init.sh"
      provides: "Shell script that waits for Redpanda health then creates driver.location topic"
      contains: "PARTITIONS=$((PUSH_SERVER_REPLICAS * PARTITION_MULTIPLIER))"
    - path: ".env.example"
      provides: "Documented tunable parameters"
      contains: "DRIVER_COUNT, EMIT_INTERVAL_MS, LOCATION_SERVICE_REPLICAS, PUSH_SERVER_REPLICAS, PARTITION_MULTIPLIER, SECRET_KEY_BASE"
    - path: "observability/prometheus.yml"
      provides: "Prometheus scrape configuration"
      contains: "scrape_configs"
    - path: "observability/grafana/provisioning/datasources/prometheus.yaml"
      provides: "Grafana auto-provisioned Prometheus datasource"
      contains: "type: prometheus"
  key_links:
    - from: "infra/redpanda-init.sh"
      to: "docker-compose.yml redpanda-init service"
      via: "volume mount and entrypoint"
      pattern: "entrypoint.*redpanda-init.sh"
    - from: "docker-compose.yml application services"
      to: "redpanda-init"
      via: "depends_on: condition: service_completed_successfully"
      pattern: "service_completed_successfully"
    - from: "docker-compose.yml application services"
      to: "redis"
      via: "depends_on: condition: service_healthy"
      pattern: "service_healthy"
    - from: "observability/prometheus.yml"
      to: "prometheus service in docker-compose.yml"
      via: "volume mount"
      pattern: "./observability/prometheus.yml:/etc/prometheus/prometheus.yml"
---

<objective>
Create the full Docker Compose infrastructure skeleton: all base services defined, Redpanda initialized with the correct topic via an init container, Redis health-checked, config externalized through .env, and Prometheus/Grafana provisioning stubs in place.

Purpose: Every subsequent phase builds services that plug into this compose file. Getting startup ordering and health checks right here prevents cascading failures in all downstream phases.
Output: A working `docker-compose.yml` that brings up all infrastructure services cleanly with a single `docker compose up --build`.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory skeleton and environment files</name>
  <files>
    .env.example
    .env
    location-service/.gitkeep
    push-server/.gitkeep
    simulator/.gitkeep
    nginx/.gitkeep
    stress/.gitkeep
    infra/redpanda-init.sh
    observability/prometheus.yml
    observability/grafana/provisioning/datasources/prometheus.yaml
    observability/grafana/provisioning/dashboards/dashboards.yaml
    observability/grafana/dashboards/.gitkeep
  </files>
  <action>
Create the full directory layout as specified in CONTEXT.md. All service directories get a `.gitkeep` so git tracks them (actual service code comes in later phases).

**`.env.example`** — committed, documents all tunables with inline comments:
```
# === Driver Simulator ===
DRIVER_COUNT=10               # Number of concurrent simulated drivers
EMIT_INTERVAL_MS=1000         # How often each driver emits a location update (ms)

# === Scaling ===
LOCATION_SERVICE_REPLICAS=2   # How many location-service replicas to run
PUSH_SERVER_REPLICAS=2        # How many push-server replicas to run
PARTITION_MULTIPLIER=2        # Partition count = PUSH_SERVER_REPLICAS × PARTITION_MULTIPLIER

# === Security ===
# For local dev only — replace in production
SECRET_KEY_BASE=dev_only_random_string_replace_in_production_abc123xyz789
```

**`.env`** — copy of `.env.example` values (this file is gitignored; create `.gitignore` entry if none exists). Add `/.env` to `.gitignore` root.

**`infra/redpanda-init.sh`** — shell script, executable (`chmod +x`):
```bash
#!/bin/bash
set -e

echo "Waiting for Redpanda broker..."
until rpk cluster health --brokers redpanda:9092 2>&1 | grep -q "Healthy: true"; do
  echo "  Broker not ready — retrying in 2s..."
  sleep 2
done
echo "Broker healthy."

# IMPORTANT: Docker Compose interpolation does NOT support arithmetic.
# Partition count MUST be computed here in the shell script.
PARTITIONS=$(( PUSH_SERVER_REPLICAS * PARTITION_MULTIPLIER ))
echo "Creating topic driver.location with ${PARTITIONS} partitions..."

rpk topic create driver.location \
  --partitions "${PARTITIONS}" \
  --replicas 1 \
  --brokers redpanda:9092 \
  --if-not-exists

echo "Init complete."
```

**`observability/prometheus.yml`** — minimal valid Prometheus config that scrapes itself; later phases add scrape targets:
```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
```

**`observability/grafana/provisioning/datasources/prometheus.yaml`** — auto-provisions Prometheus as default datasource:
```yaml
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
```

**`observability/grafana/provisioning/dashboards/dashboards.yaml`** — points Grafana at the dashboards directory (dashboards added in Phase 7):
```yaml
apiVersion: 1
providers:
  - name: mototaxi
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
```
  </action>
  <verify>
    <automated>ls infra/redpanda-init.sh .env.example .env observability/prometheus.yml observability/grafana/provisioning/datasources/prometheus.yaml observability/grafana/provisioning/dashboards/dashboards.yaml && grep -cE "DRIVER_COUNT|EMIT_INTERVAL_MS|LOCATION_SERVICE_REPLICAS|PUSH_SERVER_REPLICAS|PARTITION_MULTIPLIER|SECRET_KEY_BASE" .env.example</automated>
  </verify>
  <done>All 6 env vars present in .env.example; all listed files exist; infra/redpanda-init.sh is executable and contains the shell arithmetic line.</done>
</task>

<task type="auto">
  <name>Task 2: Write docker-compose.yml with all infrastructure services and startup ordering</name>
  <files>docker-compose.yml</files>
  <action>
Create `docker-compose.yml` at project root. This is the single source of truth for all service orchestration. Follow every constraint from CONTEXT.md and RESEARCH.md exactly.

**Port mapping (host:container) — only expose these 4:**
- Nginx: `80:80`
- Grafana: `3000:3000`
- Redpanda Console: `8080:8080`
- Prometheus: `9090:9090`

**Internal only (no host mapping):** location-service:8080, push-server:4000, redis:6379, redpanda:9092

**Network:** single `mototaxi` bridge network; all services join it.

**Services to include:**

1. **`redpanda`** — `redpandadata/redpanda:v25.2.7`, `restart: unless-stopped`
   - command flags: `redpanda start --smp 1 --memory 1G --overprovisioned --node-id 0 --kafka-addr PLAINTEXT://0.0.0.0:9092 --advertise-kafka-addr PLAINTEXT://redpanda:9092 --pandaproxy-addr 0.0.0.0:8082 --advertise-pandaproxy-addr localhost:8082 --schema-registry-addr 0.0.0.0:8081 --advertise-schema-registry-addr http://localhost:8081 --rpc-addr redpanda:33145 --advertise-rpc-addr redpanda:33145 --mode dev-container`
   - healthcheck: `["CMD-SHELL", "rpk cluster health --brokers localhost:9092 | grep -q 'Healthy: true' || exit 1"]`, interval 10s, timeout 5s, retries 10, start_period 15s
   - No host port mapping (9092 stays internal)

2. **`redpanda-init`** — `redpandadata/redpanda:v25.2.7` (no restart policy — must exit cleanly)
   - `depends_on: redpanda: condition: service_healthy`
   - `entrypoint: ["/bin/bash", "/infra/redpanda-init.sh"]`
   - `environment: PUSH_SERVER_REPLICAS: ${PUSH_SERVER_REPLICAS:-2}` and `PARTITION_MULTIPLIER: ${PARTITION_MULTIPLIER:-2}`
   - `volumes: - ./infra/redpanda-init.sh:/infra/redpanda-init.sh:ro`

3. **`redpanda-console`** — `redpandadata/console:v3.3.2`, `restart: unless-stopped`
   - `depends_on: redpanda-init: condition: service_completed_successfully`
   - `environment: KAFKA_BROKERS: redpanda:9092`
   - `ports: - "8080:8080"`

4. **`redis`** — `redis:7-alpine`, `restart: unless-stopped`
   - healthcheck: `["CMD", "redis-cli", "ping"]`, interval 5s, timeout 3s, retries 10, start_period 5s

5. **`prometheus`** — `prom/prometheus:v2.53.0`, `restart: unless-stopped`
   - command: `--config.file=/etc/prometheus/prometheus.yml --web.enable-remote-write-receiver --enable-feature=native-histograms`
   - `volumes: - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro`
   - `ports: - "9090:9090"`

6. **`grafana`** — `grafana/grafana:11.4.0`, `restart: unless-stopped`
   - `depends_on: prometheus` (plain depends_on is fine — grafana retries datasource connections)
   - `environment: GF_SECURITY_ADMIN_PASSWORD: admin`
   - volumes: mount provisioning dir and dashboards dir
     - `./observability/grafana/provisioning:/etc/grafana/provisioning:ro`
     - `./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro`
   - `ports: - "3000:3000"`

7. **`kafka-exporter`** — `danielqsj/kafka-exporter:latest`, `platform: linux/amd64`, `restart: unless-stopped`
   - `depends_on: redpanda-init: condition: service_completed_successfully`
   - `command: --kafka.server=redpanda:9092`

8. **`cadvisor`** — `gcr.io/cadvisor/cadvisor:latest`, `platform: linux/amd64`, `restart: unless-stopped`
   - volumes: `/:/rootfs:ro`, `/var/run:/var/run:rw`, `/sys:/sys:ro`, `/var/lib/docker/:/var/lib/docker:ro`

9. **`nginx`** — `nginx:alpine`, `restart: unless-stopped`
   - `ports: - "80:80"`
   - volumes: `./nginx:/etc/nginx/conf.d:ro` (populated in Phase 5; leave nginx dir empty for now — nginx will use default config until Phase 5)
   - NOTE: Since nginx dir is empty, do NOT mount it yet — nginx will fail if conf.d is mounted empty with a directory. Instead, skip the volume mount in this phase. Add it in Phase 5 when nginx.conf exists.

**Startup ordering chain:**
- `redpanda` → (healthy) → `redpanda-init` → (completed_successfully) → `redpanda-console`, `kafka-exporter`
- `redis` → (healthy) → [services that need redis — location-service, push-server, simulator in later phases]
- All services join `mototaxi` network

**Networks block:**
```yaml
networks:
  mototaxi:
    driver: bridge
```
  </action>
  <verify>
    <automated>cd /Users/iedo/Study/mototaxi && docker compose config --quiet</automated>
  </verify>
  <done>`docker compose config --quiet` exits 0 with no errors. The compose file references all 9 services, uses correct port mappings (only 80, 3000, 8080, 9090 exposed to host), and declares the mototaxi network.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. YAML syntax valid: `docker compose config --quiet`
2. All 6 env vars present: `grep -cE "DRIVER_COUNT|EMIT_INTERVAL_MS|LOCATION_SERVICE_REPLICAS|PUSH_SERVER_REPLICAS|PARTITION_MULTIPLIER|SECRET_KEY_BASE" .env.example` returns 6
3. Init script has shell arithmetic: `grep -q 'PARTITIONS=\$\((.*)\)' infra/redpanda-init.sh`
4. Service count correct: `docker compose config --services | wc -l` returns 9
5. No arithmetic in compose file: `grep -v '#' docker-compose.yml | grep -v 'PARTITIONS\$' | grep '\*'` returns nothing
</verification>

<success_criteria>
- `docker compose config --quiet` exits 0
- 9 services defined: redpanda, redpanda-init, redpanda-console, redis, prometheus, grafana, kafka-exporter, cadvisor, nginx
- Only ports 80, 3000, 8080, 9090 mapped to host
- `infra/redpanda-init.sh` exists and contains `PARTITIONS=$(( PUSH_SERVER_REPLICAS * PARTITION_MULTIPLIER ))`
- `.env.example` contains all 6 tunable parameters with inline comments
- `observability/prometheus.yml` and Grafana provisioning YAMLs exist and are valid YAML
- `depends_on` conditions use `service_healthy` or `service_completed_successfully` (not plain `depends_on`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
