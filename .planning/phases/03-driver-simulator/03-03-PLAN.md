---
phase: 03-driver-simulator
plan: 03
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - simulator/internal/geo/geo.go
  - simulator/internal/geo/geo_test.go
autonomous: true
requirements:
  - SIM-02

must_haves:
  truths:
    - "Bearing() returns correct compass degrees for known point pairs (N=0°, E=90°, S=180°, W=270°)"
    - "StepToward() moves the correct fractional distance toward destination in one tick"
    - "StepToward() never returns a point outside the São Paulo bounding box (lat -23.65..-23.45, lng -46.75..-46.55)"
    - "Arrived() returns true when within 10m of destination, false otherwise"
    - "RandomPoint() always returns a point within the bbox"
    - "RandomSpeed() always returns a value in [20, 60) km/h"
  artifacts:
    - path: "simulator/internal/geo/geo.go"
      provides: "Geographic math: Bearing, DistanceKm, StepToward, Arrived, RandomPoint, RandomSpeed, Point"
      exports: ["Point", "Bearing", "DistanceKm", "StepToward", "Arrived", "RandomPoint", "RandomSpeed"]
  key_links:
    - from: "simulator/internal/emitter/emitter.go"
      to: "simulator/internal/geo/geo.go"
      via: "geo.Bearing(), geo.StepToward(), geo.Arrived(), geo.RandomPoint(), geo.RandomSpeed()"
      pattern: "geo\\."
---

<objective>
Implement the geographic math package: haversine distance, atan2 bearing, incremental StepToward, bbox clamping, arrival detection, and random point/speed generators.

Purpose: SIM-02 — each driver goroutine needs correct bearing and movement math to emit realistic GPS updates within the São Paulo bounding box.
Output: simulator/internal/geo/geo.go with all four geo tests passing GREEN.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-driver-simulator/03-CONTEXT.md
@.planning/phases/03-driver-simulator/03-RESEARCH.md
@.planning/phases/03-driver-simulator/03-01-SUMMARY.md

<interfaces>
<!-- São Paulo bounding box constants (from REQUIREMENTS.md and CONTEXT.md) -->
// lat: -23.65 to -23.45 (north is less negative = larger value)
// lng: -46.75 to -46.55

<!-- All math uses stdlib only: math.Sin, math.Cos, math.Atan2, math.Sqrt, math.Max, math.Min, math.Mod -->
<!-- math/rand.Float64() for random generation — no seed needed in Go 1.20+ (auto-seeded) -->

<!-- Haversine formula reference (from RESEARCH.md Pattern 5) -->
// DistanceKm between two Points using haversine
// Bearing in degrees [0, 360) using atan2
// StepToward: move fraction = stepKm / distKm, then clamp to bbox
// Arrived: distanceKm < 0.01 (within ~10 meters)
// clamp: math.Max(min, math.Min(max, value)) for both lat and lng
</interfaces>
</context>

<feature>
  <name>Geographic math package (SIM-02)</name>
  <files>simulator/internal/geo/geo.go, simulator/internal/geo/geo_test.go</files>
  <behavior>
    RED → GREEN cycle:

    TestBearing:
    - Due-north pair: Bearing({Lat:-23.55, Lng:-46.65}, {Lat:-23.45, Lng:-46.65}) ≈ 0.0° (tolerance ±1°)
    - Due-east pair: Bearing({-23.55, -46.65}, {-23.55, -46.55}) ≈ 90.0° (tolerance ±1°)
    - Due-south pair: Bearing({-23.45, -46.65}, {-23.55, -46.65}) ≈ 180.0° (tolerance ±1°)
    - Due-west pair: Bearing({-23.55, -46.55}, {-23.55, -46.65}) ≈ 270.0° (tolerance ±1°)

    TestStepToward:
    - A = {-23.55, -46.65}, B = {-23.45, -46.65} (due north, ~11.1 km apart)
    - StepToward(A, B, 60.0, 1.0) moves ~60/3600 = 0.01667 km toward B
    - Result.Lat > A.Lat (moved north), Result.Lat < B.Lat (not past B)
    - Result.Lng ≈ A.Lng (no east-west drift on due-north path)
    - When step >= distance: returns clamped(B) (arrived case)

    TestBboxClamp:
    - StepToward toward a destination outside bbox stays within bbox after clamp
    - Directly: clamp({-25.0, -47.0}) == {-23.65, -46.75} (south-west corner)
    - clamp({-22.0, -46.0}) == {-23.45, -46.55} (north-east corner)

    TestArrived:
    - Arrived(p, p) returns true (same point)
    - Arrived(A, B) where DistanceKm(A,B) = 0.005 km returns true (< 0.01 threshold)
    - Arrived(A, B) where DistanceKm(A,B) = 0.5 km returns false

    TestRandomPoint (property test, 100 iterations):
    - RandomPoint() result is always within bbox

    TestRandomSpeed (property test, 100 iterations):
    - RandomSpeed() result is always in [20.0, 60.0)
  </behavior>
  <implementation>
    Implement simulator/internal/geo/geo.go following the exact formulas from RESEARCH.md Pattern 5.
    All implementation uses stdlib math only — no external geo library.

    Key implementation notes:
    - Bearing: all trig in radians, return math.Mod(θ+360, 360) to normalize to [0, 360)
    - DistanceKm: standard haversine with earthRadiusKm = 6371.0
    - StepToward: compute stepKm = speedKmh * elapsedSec / 3600.0; if stepKm >= distKm return clamp(b); else interpolate linearly and clamp
    - clamp() applied unconditionally on every returned Point (IEEE 754 rounding safety)
    - Arrived threshold: 0.01 km (~10 meters)
    - RandomPoint: latMin + rand.Float64()*(latMax-latMin), same for lng
    - RandomSpeed: 20.0 + rand.Float64()*40.0 gives [20.0, 60.0)

    Update geo_test.go stubs to call real implementations. All tests must pass GREEN.

    Run after implementation:
    ```bash
    cd /Users/iedo/Study/mototaxi/simulator && go test ./internal/geo/... -v
    ```
  </implementation>
</feature>

<verification>
  <automated>cd /Users/iedo/Study/mototaxi/simulator && go test ./internal/geo/... -run "TestBearing|TestStepToward|TestBboxClamp|TestArrived|TestRandom" -v</automated>
</verification>

<success_criteria>
- TestBearing passes all four cardinal directions within ±1° tolerance
- TestStepToward passes: correct fractional movement and arrived-case handling
- TestBboxClamp passes: clamped values never exceed bbox bounds
- TestArrived passes: correct threshold detection
- TestRandomPoint and TestRandomSpeed pass 100-iteration property tests
- `go test ./internal/geo/... -v` shows only PASS lines
</success_criteria>

<output>
After completion, create `.planning/phases/03-driver-simulator/03-03-SUMMARY.md`
</output>
