---
phase: 03-driver-simulator
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified:
  - simulator/cmd/simulator/main.go
  - simulator/Dockerfile
  - docker-compose.yml
  - .env.example
autonomous: false
requirements:
  - SIM-04
  - SIM-05

must_haves:
  truths:
    - "docker build -t simulator-test ./simulator completes successfully (multi-stage FROM scratch)"
    - "DRIVER_COUNT and EMIT_INTERVAL_MS in .env are read by the simulator on startup"
    - "docker compose up brings up the simulator service; Redis contains customer:* and driver:* keys"
    - "location-service logs show POST /location requests arriving from the simulator"
    - "Changing DRIVER_COUNT=3 and restarting simulator produces exactly 6 Redis assignment keys"
  artifacts:
    - path: "simulator/cmd/simulator/main.go"
      provides: "Binary entrypoint: config, Redis ping+seed, goroutine launch, graceful shutdown"
    - path: "simulator/Dockerfile"
      provides: "Multi-stage FROM scratch image"
      contains: "FROM scratch"
    - path: "docker-compose.yml"
      provides: "simulator service block"
      contains: "simulator:"
    - path: ".env.example"
      provides: "LOCATION_SERVICE_URL documented"
      contains: "LOCATION_SERVICE_URL"
  key_links:
    - from: "simulator/cmd/simulator/main.go"
      to: "simulator/internal/seeder"
      via: "seeder.SeedAssignments(ctx, rdb, driverCount)"
      pattern: "seeder\\.SeedAssignments"
    - from: "simulator/cmd/simulator/main.go"
      to: "simulator/internal/emitter"
      via: "go emitter.RunDriver(ctx, i, client, locationURL, emitIntervalMs)"
      pattern: "emitter\\.RunDriver"
    - from: "docker-compose.yml"
      to: "simulator/"
      via: "build: context: ./simulator"
      pattern: "context: ./simulator"
---

<objective>
Wire everything together: main.go connects seeder + emitter, Dockerfile packages as FROM scratch binary, docker-compose adds the simulator service, and a human smoke test confirms Redis keys and location-service logs.

Purpose: SIM-04 and SIM-05 — the simulator must be deployable as a Docker container and respect DRIVER_COUNT/EMIT_INTERVAL_MS from env.
Output: Deployable simulator service integrated into the full docker-compose stack.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-driver-simulator/03-CONTEXT.md
@.planning/phases/03-driver-simulator/03-RESEARCH.md
@.planning/phases/03-driver-simulator/03-04-SUMMARY.md

<interfaces>
<!-- Existing docker-compose.yml location-service block (for depends_on reference) -->
```yaml
location-service:
  build:
    context: ./location-service
    dockerfile: Dockerfile
  restart: unless-stopped
  depends_on:
    redis:
      condition: service_healthy
    redpanda-init:
      condition: service_completed_successfully
  environment:
    REDIS_ADDR: redis:6379
    KAFKA_ADDR: redpanda:9092
    KAFKA_TOPIC: driver.location
  networks:
    - mototaxi
```

<!-- Existing .env.example variables already present -->
// DRIVER_COUNT=10
// EMIT_INTERVAL_MS=1000
// (LOCATION_SERVICE_URL must be ADDED by this plan)

<!-- location-service Dockerfile pattern (from scratch) -->
```dockerfile
FROM golang:1.24-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
    go build -ldflags="-s -w" -o simulator ./cmd/simulator
FROM scratch
COPY --from=builder /app/simulator /simulator
ENTRYPOINT ["/simulator"]
```
<!-- Note: No EXPOSE — simulator has no inbound HTTP server -->

<!-- seeder and emitter package signatures (implemented in Plans 02-04) -->
```go
// seeder.SeedAssignments(ctx, rdb, n) error
// emitter.RunDriver(ctx, id, client, locationURL, intervalMs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: main.go wiring + Dockerfile + docker-compose integration</name>
  <files>
    simulator/cmd/simulator/main.go,
    simulator/Dockerfile,
    docker-compose.yml,
    .env.example
  </files>
  <behavior>
    - TestEnvConfig: os.Getenv unset → envOr returns default; set → returns set value; empty string → returns default
    - TestMustInt: "10" → 10; "0" → fatal; "-1" → fatal; "" → fatal (after envOr returns non-empty default, but if called with "" directly → fatal); "abc" → fatal
  </behavior>
  <action>
    Create simulator/cmd/simulator/main.go:

    1. envOr(key, def string) string — identical to location-service pattern
    2. mustInt(s string) int — strconv.Atoi; if err != nil || v <= 0 { log.Fatalf(...) }; return v
    3. pingWithRetry(rdb *redis.Client) — 10 attempts × 2s sleep, log.Fatalf after max attempts
    4. main():
       - Read config: redisAddr, locationURL, driverCount, emitIntervalMs via mustInt(envOr(...))
       - Build rdb: redis.NewClient(&redis.Options{Addr: redisAddr})
       - pingWithRetry(rdb)
       - seeder.SeedAssignments(context.Background(), rdb, driverCount) — log.Fatalf on error
       - Set up signal.NotifyContext for SIGINT/SIGTERM graceful shutdown
       - Create shared http.Client{Timeout: 5 * time.Second}
       - Launch driverCount goroutines: each calls emitter.RunDriver(ctx, i, client, locationURL, emitIntervalMs) in its own go func with wg.Add/Done
       - wg.Wait() — blocks until all goroutines return (after ctx cancel)
       - log.Println("simulator stopped cleanly")

    Create simulator/Dockerfile using the FROM scratch multi-stage pattern.
    Build target: ./cmd/simulator (not ./cmd/simulator/main.go — go build takes package path).
    Output binary name: simulator. No EXPOSE.

    Update docker-compose.yml — add simulator service block after location-service:
    ```yaml
    simulator:
      build:
        context: ./simulator
        dockerfile: Dockerfile
      restart: unless-stopped
      depends_on:
        redis:
          condition: service_healthy
        location-service:
          condition: service_started
      environment:
        REDIS_ADDR: redis:6379
        LOCATION_SERVICE_URL: ${LOCATION_SERVICE_URL:-http://location-service:8080}
        DRIVER_COUNT: ${DRIVER_COUNT:-10}
        EMIT_INTERVAL_MS: ${EMIT_INTERVAL_MS:-1000}
      networks:
        - mototaxi
    ```

    Update .env.example — add after existing simulator variables:
    LOCATION_SERVICE_URL=http://location-service:8080

    Update simulator_test or cmd/simulator tests to cover envOr and mustInt (per SIM-05 test map entry 3-05-01). Add simulator/cmd/simulator/main_test.go with TestEnvConfig and TestMustInt.
    Note: mustInt with fatal behavior is tested via subprocess testing (exec.Command) or by testing the envOr+Atoi logic directly on the non-fatal path; use table-driven tests for the valid cases.
  </action>
  <verify>
    <automated>cd /Users/iedo/Study/mototaxi/simulator && go build ./... && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <done>
    - simulator/cmd/simulator/main.go compiles with no errors
    - simulator/Dockerfile exists with FROM scratch final stage
    - docker-compose.yml contains simulator service block with correct depends_on
    - .env.example contains LOCATION_SERVICE_URL
    - go build ./... and go test ./... both succeed
  </done>
</task>

<task type="auto">
  <name>Task 2: Docker image build verification</name>
  <files>simulator/Dockerfile</files>
  <action>
    Build the simulator Docker image to verify the multi-stage FROM scratch Dockerfile produces a working binary.

    Run:
    ```bash
    docker build -t simulator-test /Users/iedo/Study/mototaxi/simulator
    ```

    If the build fails, diagnose and fix. Common issues:
    - CGO_ENABLED=0 not set → binary not fully static, FROM scratch fails
    - Wrong build target path (use ./cmd/simulator not ./cmd/simulator/main.go)
    - go.sum out of date → run `cd simulator && go mod tidy` first

    Verify image was built:
    ```bash
    docker image inspect simulator-test --format "{{.Size}}"
    ```
    Expected: < 15MB (FROM scratch with no OS layer)
  </action>
  <verify>
    <automated>docker build -t simulator-test /Users/iedo/Study/mototaxi/simulator 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `docker build -t simulator-test ./simulator` exits 0
    - Image size is under 15MB
    - No error output from the build
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full simulator service integrated into docker-compose stack:
    - simulator/cmd/simulator/main.go (seeder + goroutine launch + graceful shutdown)
    - simulator/Dockerfile (FROM scratch multi-stage, ~5MB)
    - docker-compose.yml updated with simulator service block
    - .env.example updated with LOCATION_SERVICE_URL
    - All unit tests passing (seeder, geo, emitter, config)
  </what-built>
  <how-to-verify>
    1. Start the full stack:
       ```bash
       cd /Users/iedo/Study/mototaxi && docker compose up --build -d
       ```

    2. Wait ~10 seconds for services to start, then check Redis assignment keys:
       ```bash
       docker compose exec redis redis-cli keys "customer:*" | head -5
       docker compose exec redis redis-cli get "customer:customer-1:driver"
       docker compose exec redis redis-cli get "driver:driver-1:customer"
       ```
       Expected: "driver-1" and "customer-1" respectively

    3. Verify location-service is receiving emit requests from simulator:
       ```bash
       docker compose logs --follow location-service 2>&1 | head -20
       ```
       Expected: POST /location log entries appearing at ~1/sec per driver

    4. Verify DRIVER_COUNT change works — set DRIVER_COUNT=3 in .env, restart simulator:
       ```bash
       docker compose restart simulator
       sleep 5
       docker compose exec redis redis-cli keys "*:driver" | wc -l
       ```
       Expected: 3 keys (customer:customer-1:driver through customer:customer-3:driver)

    5. Stop the stack:
       ```bash
       docker compose down
       ```
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All unit tests pass:
```bash
cd /Users/iedo/Study/mototaxi/simulator && go test ./...
```

Docker image builds:
```bash
docker build -t simulator-test /Users/iedo/Study/mototaxi/simulator
```

Stack integration verified by human smoke test checkpoint.
</verification>

<success_criteria>
- `go test ./...` from simulator/ shows all green
- `docker build -t simulator-test ./simulator` exits 0 and produces image < 15MB
- `docker compose up --build` brings up simulator service cleanly
- Redis contains `customer:customer-N:driver` and `driver:driver-N:customer` keys for all N drivers
- location-service logs show POST /location requests at configured EMIT_INTERVAL_MS cadence
- DRIVER_COUNT change + restart changes the number of active goroutines and Redis keys
</success_criteria>

<output>
After completion, create `.planning/phases/03-driver-simulator/03-05-SUMMARY.md`
</output>
