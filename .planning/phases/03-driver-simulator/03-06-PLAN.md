---
phase: 03-driver-simulator
plan: 06
type: gap_closure
wave: 5
depends_on:
  - 03-05
files_modified:
  - simulator/internal/emitter/emitter.go
  - simulator/internal/geo/geo.go
autonomous: true
requirements:
  - SIM-02

must_haves:
  truths:
    - "saoPauloBbox in emitter.go uses MinLat:-23.65, MaxLat:-23.45, MinLng:-46.75, MaxLng:-46.55"
    - "StepToward comment in geo.go does not claim clamping behavior"
    - "go test ./... from simulator/ still passes after changes"
  artifacts:
    - path: "simulator/internal/emitter/emitter.go"
      provides: "Correct São Paulo bounding box constants matching SIM-02"
      contains: "MinLat: -23.65"
    - path: "simulator/internal/geo/geo.go"
      provides: "Accurate StepToward doc comment"
  key_links:
    - from: "simulator/internal/emitter/emitter.go"
      to: "simulator/internal/geo"
      via: "geo.RandomPoint(saoPauloBbox)"
      pattern: "saoPauloBbox"
---

<objective>
Close the two SIM-02 verification gaps found in 03-VERIFICATION.md:
1. Fix saoPauloBbox constants in emitter.go to use the exact required boundary (lat -23.65→-23.45, lng -46.75→-46.55).
2. Fix the misleading StepToward comment in geo.go that falsely claims bbox clamping.

Purpose: SIM-02 — drivers must move within the specified São Paulo bounding box, never outside it.
Output: emitter.go and geo.go with corrected values; all existing tests still green.
</objective>

<execution_context>
@/Users/iedo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-driver-simulator/03-CONTEXT.md
@.planning/phases/03-driver-simulator/03-VERIFICATION.md
@.planning/phases/03-driver-simulator/03-05-SUMMARY.md

<gap_details>
<!-- From 03-VERIFICATION.md -->

Gap 1 — Blocker (SIM-02): Wrong bbox in emitter.go
  File: simulator/internal/emitter/emitter.go, lines 17-22
  Current:  saoPauloBbox = geo.Bbox{MinLat: -23.7, MaxLat: -23.4, MinLng: -46.9, MaxLng: -46.3}
  Required: saoPauloBbox = geo.Bbox{MinLat: -23.65, MaxLat: -23.45, MinLng: -46.75, MaxLng: -46.55}
  Source:   SIM-02 + ROADMAP success criterion 2

Gap 2 — Warning: Misleading StepToward comment in geo.go
  File: simulator/internal/geo/geo.go, line ~48
  Current comment: "Returns the new position clamped to bbox"
  Issue: StepToward does not accept a Bbox parameter and does not call BboxClamp
  Fix:   Update comment to accurately describe what StepToward does: interpolates position toward dst by speedKmh over intervalMs, without clamping
</gap_details>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix saoPauloBbox constants in emitter.go</name>
  <files>simulator/internal/emitter/emitter.go</files>
  <action>
    Read simulator/internal/emitter/emitter.go to locate the saoPauloBbox constant block.

    Change the constant values from:
      MinLat: -23.7, MaxLat: -23.4, MinLng: -46.9, MaxLng: -46.3

    To:
      MinLat: -23.65, MaxLat: -23.45, MinLng: -46.75, MaxLng: -46.55

    Do NOT change anything else in the file — this is a targeted constant-value fix only.
  </action>
  <verify>
    <automated>grep "MinLat" /Users/iedo/Study/mototaxi/simulator/internal/emitter/emitter.go</automated>
  </verify>
  <done>
    - emitter.go contains MinLat: -23.65, MaxLat: -23.45, MinLng: -46.75, MaxLng: -46.55
    - No other changes to the file
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix misleading StepToward comment in geo.go</name>
  <files>simulator/internal/geo/geo.go</files>
  <action>
    Read simulator/internal/geo/geo.go to locate the StepToward function and its comment around line 47-48.

    Remove or correct the phrase "clamped to bbox" from the comment. The function interpolates src toward dst by one step based on speedKmh and intervalMs — it does not apply any bbox clamping.

    Replace the misleading comment with an accurate one, for example:
    "// StepToward returns the new position after moving from src toward dst at speedKmh over intervalMs."

    Do NOT change the function signature or implementation — comment fix only.
  </action>
  <verify>
    <automated>grep -n "clamped\|StepToward" /Users/iedo/Study/mototaxi/simulator/internal/geo/geo.go</automated>
  </verify>
  <done>
    - geo.go StepToward comment no longer contains "clamped to bbox" or any false clamping claim
    - Function signature and body unchanged
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run all simulator tests</name>
  <files></files>
  <action>
    Run the full simulator test suite to confirm both changes are non-breaking:

    ```bash
    cd /Users/iedo/Study/mototaxi/simulator && go test ./...
    ```

    All tests must pass (seeder, geo, emitter, config). If any test fails, investigate and fix — do NOT proceed until green.
  </action>
  <verify>
    <automated>cd /Users/iedo/Study/mototaxi/simulator && go test ./... 2>&1</automated>
  </verify>
  <done>
    - `go test ./...` exits 0
    - All packages report "ok"
    - No new failures introduced
  </done>
</task>

</tasks>

<verification>
Confirm bbox fix:
```bash
grep "MinLat\|MaxLat\|MinLng\|MaxLng" /Users/iedo/Study/mototaxi/simulator/internal/emitter/emitter.go
```
Expected: MinLat: -23.65, MaxLat: -23.45, MinLng: -46.75, MaxLng: -46.55

Confirm comment fix:
```bash
grep -n "clamped\|StepToward" /Users/iedo/Study/mototaxi/simulator/internal/geo/geo.go
```
Expected: "clamped" not present in output; StepToward comment is accurate.

Confirm no regressions:
```bash
cd /Users/iedo/Study/mototaxi/simulator && go test ./...
```
Expected: all packages ok.
</verification>

<success_criteria>
- emitter.go saoPauloBbox has MinLat:-23.65, MaxLat:-23.45, MinLng:-46.75, MaxLng:-46.55
- geo.go StepToward comment does not claim bbox clamping
- `go test ./...` passes with no failures
- SIM-02 verification gap resolved: drivers are constrained to the correct São Paulo bounding box
</success_criteria>

<output>
After completion, create `.planning/phases/03-driver-simulator/03-06-SUMMARY.md`
</output>
