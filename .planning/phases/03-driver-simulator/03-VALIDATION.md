---
phase: 3
slug: driver-simulator
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-06
---

# Phase 3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go stdlib `testing` (no external framework) |
| **Config file** | none — `go test ./...` from `simulator/` directory |
| **Quick run command** | `cd simulator && go test ./internal/...` |
| **Full suite command** | `cd simulator && go test ./...` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd simulator && go test ./internal/...`
- **After every plan wave:** Run `cd simulator && go test ./...`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 3-01-01 | 01 | 0 | SIM-01 | unit | `cd simulator && go test ./internal/seeder/... -run TestSeedAssignments -v` | ❌ W0 | ⬜ pending |
| 3-01-02 | 01 | 0 | SIM-01 | unit | `cd simulator && go test ./internal/seeder/... -run TestSeedIdempotent -v` | ❌ W0 | ⬜ pending |
| 3-02-01 | 02 | 0 | SIM-02 | unit | `cd simulator && go test ./internal/geo/... -run TestBearing -v` | ❌ W0 | ⬜ pending |
| 3-02-02 | 02 | 0 | SIM-02 | unit | `cd simulator && go test ./internal/geo/... -run TestStepToward -v` | ❌ W0 | ⬜ pending |
| 3-02-03 | 02 | 0 | SIM-02 | unit | `cd simulator && go test ./internal/geo/... -run TestBboxClamp -v` | ❌ W0 | ⬜ pending |
| 3-02-04 | 02 | 0 | SIM-02 | unit | `cd simulator && go test ./internal/geo/... -run TestArrived -v` | ❌ W0 | ⬜ pending |
| 3-03-01 | 03 | 0 | SIM-03 | unit | `cd simulator && go test ./internal/emitter/... -run TestEmitPayload -v` | ❌ W0 | ⬜ pending |
| 3-03-02 | 03 | 0 | SIM-03 | unit | `cd simulator && go test ./internal/emitter/... -run TestEmitNon200 -v` | ❌ W0 | ⬜ pending |
| 3-04-01 | 04 | 1 | SIM-04 | smoke | `docker build -t simulator-test ./simulator` | ❌ W0 | ⬜ pending |
| 3-05-01 | 05 | 0 | SIM-05 | unit | `cd simulator && go test ./... -run TestEnvConfig -v` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `simulator/internal/seeder/seeder_test.go` — stubs for SIM-01 (uses miniredis)
- [ ] `simulator/internal/geo/geo_test.go` — stubs for SIM-02 (pure math, no external deps)
- [ ] `simulator/internal/emitter/emitter_test.go` — stubs for SIM-03 (uses `httptest.NewServer`)
- [ ] `simulator/go.mod` — module scaffold with go-redis/v9 and miniredis/v2
- [ ] `simulator/go.sum` — generated by `go mod tidy`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Redis keys visible in running stack after `docker compose up` | SIM-01 | Requires live Docker stack | `docker compose up -d && docker compose exec redis redis-cli keys "customer:*" \| head -5` |
| Goroutines emit at correct interval (observable via location-service logs) | SIM-03 | Requires live integration | `docker compose logs --follow location-service \| grep driver-1` — verify ~1 log entry/sec |
| DRIVER_COUNT=3 restart yields exactly 3 active goroutines | SIM-05 | Requires Docker restart | Change `.env` DRIVER_COUNT=3, `docker compose restart simulator`, check Redis has 6 keys |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
